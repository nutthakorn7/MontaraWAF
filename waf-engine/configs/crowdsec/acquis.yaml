# CrowdSec Acquisition Configuration
# Defines where CrowdSec should read logs from

# Watch APISIX access logs (mounted from APISIX container)
filenames:
  - /var/log/apisix/access.log
  - /var/log/apisix/error.log
labels:
  type: nginx
