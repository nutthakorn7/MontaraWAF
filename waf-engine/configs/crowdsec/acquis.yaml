# CrowdSec Acquisition Configuration
# Defines where CrowdSec should read logs from

filenames:
  - /var/log/nginx/access.log
  - /var/log/nginx/error.log
labels:
  type: nginx
---
# APISIX logs (via stdout)
source: docker
container_name:
  - montara-apisix
labels:
  type: nginx
