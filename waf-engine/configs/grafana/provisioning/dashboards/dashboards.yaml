# Grafana Dashboard Provisioning

apiVersion: 1

providers:
  - name: 'Montara WAF'
    orgId: 1
    folder: 'WAF'
    folderUid: 'montara-waf'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
